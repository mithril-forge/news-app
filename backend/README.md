1. Implement TODOs
2. Update README and docs
3. Image generation:
    a) OpenAI and other models (as Perplexity) works good in UI but their API doesn't work well to search and connect images
    b) We need to solve this and find how to parametrize the API calls properly, so it searches good results OR we should generate search queries by AI, use wiki-commons API to get images
       and then use AI again to pick the best ones and corresponding ones.     